node_modules/
dist/
.env
.env.local
*.log
.firebase/
functions/lib/
.worktrees/
.DS_Store
# Spurious lockfile — there is no root package.json (packages live in frontend/functions/scripts).
/package-lock.json
# Per-user local Claude Code settings.
.claude/settings.local.json
# Local data snapshots (e.g. AppBird X-Ray corpus for scripts/seed-xray-corpus.ts).
.local/
